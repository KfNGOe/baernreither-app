export = Authority;
/**
 * *
 */
type Authority = string;
declare namespace Authority {
    let ADMIN: string;
    let REPO_MANAGER: string;
    let USER: string;
    let READ_REPO_PREFIX: string;
    let WRITE_REPO_PREFIX: string;
}
